# ------------------------------------------------------------------
# Production env. Copy to .env.production (manual VPS) OR paste into the
# Hostinger "Environment variables" field (managed deploy).
#   cp .env.production.example .env.production
# NEVER commit .env.production (it is gitignored via .env.*).
# ------------------------------------------------------------------

# --- Backend URL (sem trailing slash) ---
# Deve ser acessível DE DENTRO do container (não localhost).
# Se o backend roda no mesmo host: use o IP da máquina (não 127.0.0.1).
BACKEND_URL=http://2.24.80.138:3000
